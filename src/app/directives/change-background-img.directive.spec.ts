import { ChangeBackgroundImgDirective } from './change-background-img.directive';

describe('ChangeBackgroundImgDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeBackgroundImgDirective();
    expect(directive).toBeTruthy();
  });
});
